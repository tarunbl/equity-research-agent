anthropic>=0.49.0,<1.0
pydantic>=2.0.0,<3.0
python-dotenv>=1.0.0,<2.0
rich>=13.0.0,<14.0
yfinance>=0.2.50,<1.0
requests>=2.31.0,<3.0

# Optional: FinBERT financial sentiment classifier
# Improves news sentiment accuracy — better than general-purpose LLMs on financial text
# Install with: pip install transformers torch
# Model download: ~500MB on first use (cached locally)
# transformers>=4.30.0,<5.0
# torch>=2.0.0,<3.0
